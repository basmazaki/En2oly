import { GetAllFinshedRequests } from './get-all-finshed-requests';

describe('GetAllFinshedRequests', () => {
  it('should create an instance', () => {
    expect(new GetAllFinshedRequests()).toBeTruthy();
  });
});
