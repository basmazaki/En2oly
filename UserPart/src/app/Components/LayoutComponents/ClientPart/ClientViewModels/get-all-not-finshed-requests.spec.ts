import { GetAllNotFinshedRequests } from './get-all-not-finshed-requests';

describe('GetAllNotFinshedRequests', () => {
  it('should create an instance', () => {
    expect(new GetAllNotFinshedRequests()).toBeTruthy();
  });
});
